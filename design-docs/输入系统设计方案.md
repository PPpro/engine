# Unity 架构：
- ActionAsset
    - ActionMap： ActionScheme to Action
        - Action 抹平输入设备的差异
        - Action Binding 将 Action 绑定到不同输入设备 / 不同输入按键
            - Modifier 与 Unreal 的 Modifier 是不同的概念，Unity 的 Modifier 是用于组合键的需求，例如 shift+1
            - Interaction 对标 Unreal 的 Trigger
            - Processor 对标 Unreal 的 Modifier，Unity 支持 Processor 绑定到 Action， Binding，Control

- Player Input 组件 (低优先级)
    - ActionAsset
    - Behavior: SendMessage, BroadcastMessage, UnityEvent, C# Event
    - Camera：用于 PlayerInputManager 的 split screen 能力
    - UI Input Module

- Player Input Manager (低优先级)
    - join
    - split screen

- 状态查询支持
    - InputAction.readValue
    - Gamepad.current.leftStick.readValue

- UI 支持课题
    - Pointer Type, Navigation Type, Tracking Type
    - UI vs game Input 歧义问题
        - PointerType 歧义
        - Navigation 歧义
    - Pointer Behavior：
        - Single Mouse or Pen But Multi Touch And Track 多设备输入，后者覆盖前者
        - Single Unified Pointer，多点输入，后者覆盖前者
        - All Pointers As Is，所有输入，都以后者为准

- 测试框架

# Unreal 方案
- Input Action
- Action Mapping Context 输入映射上下文，用于区分不同场景下使用的映射表，比如行走状态，与驾驶状态，是两个不同的输入映射上下文，这样子可以避免输入冲突，也不会浪费 CPU 周期
- Modifier：Input Modifiers are pre-processors that alter the raw input values that UE4 receives before sending them on to Input Triggers.The Enhanced Input Plugin ships with a variety of Input Modifiers to perform tasks like changing the order of axes, implementing "dead zones", converting axial input to world space, and several others.
- Trigger: Input Triggers determine whether or not a user input, after passing through an optional list of Input Modifiers, should activate the corresponding Input Action within its Input Mapping Context


# 疑问
- InputAction readValue 的体验如何优化？
- Interaction 是否需要很多个类实现？
- binding button 与 interactin， processor 如何关联 ？
- 一个 action 绑定多个 interaction 的必要性是什么，冲突怎么解决？
- 运行时 键位设置的方案？ ————rebinding 方案
- Unity 的 ActionType 以及 ControlType 是否可以合并？ ————不能，ActionType 侧重于对值的处理，ControlType 侧重于值类型
- Action 和 Binding 都有 Interaction ， Unity 是怎么处理的？————先处理 binding，再处理 Action
- Button 和 Axis 绑定如何区分？

# 方案差异
- Unity Unreal 是将 Action 映射到一个 KeyCode Array，抹平了输入设备的差异
- Cocos 是将 Event 分类，再细分 Event 阶段，更拘泥于底层细节，不是一个很好的产品设计

# 关于绑定方案的差异
- Unity 对于多向输入，采用的是 BindingComposite 的方案，设计上比较强调类型对应关系
- 对于 修饰键 的绑定，也是通过 ModifierComposite 来解决，缺点是，对 Modifier 的边界不够清晰
- Unity 有细分按键层级，比如 Vector2D 的 LeftStick 是一个层级，往下是 Axis1D 的 LeftStickUp LeftStickDown LeftStickLeft LeftStickRight 等的细分层级，在绑定上的控制比较细
- Unity 没有独立的 Action 资源，但是可以暴露出 Action 属性面板

- Unreal 的方案是，将 raw input 贡献到具体轴，设计上没那么强调类型对应关系，甚至可以将 Vector3Control 绑定到 Vec2Action 上，只不过会丢失 z 轴的数据 （个人偏向于这个方案，因为这样开发者能够少一些认知概念，不用了解什么是 composite）
- 对于 修饰键 的绑定，Unreal 使用的是 trigger 方案，chorded action 来实现组合 action
- Unreal 是直接将按键层级拍平，LeftStick 拍平成 LeftStickX 和 LeftStickY
- 其他差异：Unreal Action 没有绑定配置，只有在 Context 里做绑定，设计上认为 Action 没有具体的 Context 是没有意义的；Action 支持 Trigger When Paused;（个人觉得就没必要有 Action 资源）
- 问题：Chorded Action 来自哪里，是否可以来自别的 Context 配置？


# 一些想法 / 需求
- 对于没有跨平台需求的项目，有没有更简易的方案？InputSystem 的定位是什么？解决什么问题？
- Action 的优势，是可以做 ReBinding （BindingIconList 要不要提供？）
- 支持 Gamepad Cursor
- 在屏虚拟按钮，可以作为一个典型的 CustomInput 的案例
- 每个层级都能够支持自定义，CustomDevice，CustomBindingComposite
- 支持 inputRecord, 用于自动化，或者视频回放
- Unity 的 UI 是一定会接收事件的，开发者可以通过 EventSystem.current.isPointerOverGameObject 来判断要不要处理 game input（这点跟 cocos 目前的设计有差异，cocos UI 是直接吞噬事件）
- 如何支持输入设备扩展, InputDevice 模块，参考 unity Layout 模块（低优先级）


# Cocos 方案
- 

# 阻碍
- 怎么做到很好的类型映射编码体验？
- 怎么做好序列化反序列化？（键位配置数据持久化是否也是类似的问题）-- 实现一个 json parser / stringifier
- 怎么跟现有的方案做好兼容？还是直接干掉现在的方案？