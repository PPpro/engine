# 模块化遇到的问题
- tsconfig.json 里配置的 paths 字段，可以实现模块化的目的，将模块以裸说明符的方式导入，但是 vscode 有时候会以相对路径导入，体验很差
- 将模块以 npm module 的形式包装起来，vscode 对这方面的支持做的很不错，可以尝试使用这个方案
- 千万记住，引擎模块化，不是一个设计模式上的问题，是软件工程上的问题，区别在于前者是运行时解决方案，后者是预处理阶段的解决方案，经过预处理的引擎包体可以得到大幅度的优化

# 开发阶段
- 模块与模块之间，通过 npm 模块导入
  - 是否需要模块之间互相注册模块，这是个问题 ！！！（答案是不需要，只要在根目录下的 package.json 里注册一次就行了）
- 模块内部之间的 chunk 引用，警惕 vscode 总是以最短相对路径导入的特性，他可能以 index 路径导入，这样很容易循环引用（如何避免？）
  - 模块内部的 tsconfig.json 需要 exclude index.ts （见遗留问题）
  - 平台相关的接口定义在 pal.d.ts 里
  - 如果是平台相关的 chunk, 被导入的是一份声明，不是具体实现（pal.d.ts），构建的时候才会选择具体实现 
  - 其他 chunk，直接 relative import
- 模块需要用 index.d.ts 导出，如果是 index.ts 会解析不到
- 工作代码，都需要被 include，否则没办法应用 Node12 的模块解析方案 (只需要 include index.d.ts)
- 每个子模块，需要继承自根目录的 tsconfig.json，否则很多配置会失效，都是默认选项，包括 Node12 也没有应用上去 (好像没必要)

# 模块工作流
- 模块注册流程还是相对复杂了，需要实现一个脚手架，来批量更新一些模块的配置信息


# 构建阶段

# 运行时阶段

# 自动化测试阶段

# 如何实现 WASM 模块

# 遗留问题
- tsconfig eslint 这些配置怎么在模块间 共享
- package.json exports 目前还没有被 ts 支持 https://github.com/microsoft/TypeScript/issues/33079 (4.7 beta 已经支持了)
- legacyCC 如何处理 （已经划归 cc/core/global）
- player-minigame.ts 里没有支持 node12 模块
- tsconfig @types 怎么引入
- 如何解决 pal 模块解析
- 解决 input 类型问题，以及 touchManager 归属问题
- 解决 import/no-extraneous-dependencies

# 新增的导出模块
- preTransforms
- callback-invoker
- applyMixins